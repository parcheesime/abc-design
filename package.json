{
  "name": "abc-design",
  "private": true,
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "test:lighthouse": "npm run build && lhci autorun",
    "report:lighthouse": "node scripts/lighthouse-summary.cjs",
    "audit": "npm run test:lighthouse",
    "audit:report": "npm run test:lighthouse && npm run report:lighthouse"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@lhci/cli": "^0.15.1"
  }
}
